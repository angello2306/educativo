-title 'Listado de Niveles'

.row
  .col-md-6
    = link_to 'Nuevo Nivel', new_admin_level_path, { :class=>"btn btn-primary" }
  .col-md-6{:style => "text-align:right"}
    = form_tag(admin_levels_path, :method => "post", :class => "form-inline") do
      .form-group
        = text_field_tag("search[q]", params[:q], :class => "form-control")
      .form-group
        = submit_tag(t('form.btn_search'), :class => "btn btn-default")
%hr

%table.table.table-striped
  %thead
    %tr
      %th #
      %th Nombre
      %th
      %th
  %tbody
  - @levels.each do |level|
    %tr
      %td= level.id
      %td= level.name
      %td= link_to 'Editar', edit_admin_level_path(level)
      %td= link_to 'Eliminar', [:admin, level], :method => :delete, :data => { :confirm => 'Estas seguro?' }

%br
